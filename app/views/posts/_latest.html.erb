<% @posts.each do |post| %>
  <li>
    <% if post.image.exists? %>
      <%= link_to image_tag post.image.url(:medium), :align => 'center', :id => 'scaled' %>
    <% end %>

  </li>       
<% end %> 

